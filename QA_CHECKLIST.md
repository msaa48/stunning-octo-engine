# QA_CHECKLIST — مسار / masar-centers
> ⚠️ هذا الملف يُحقن مباشرة في الـ prompt بتاع `qa_autofix.py` (مرحلة 0: التأليف الذاتي). Gemini **ملزم** يتبعه.

## القاعدة الذهبية (غير قابلة للتفاوض)

**زرار/فورم/شاشة ما تتعتبرش "مُختبرة" إلا لو الاختبار بيتحقق من نتيجة منطقية حقيقية بعد التفاعل — مش مجرد إثبات إن العنصر ظاهر على الشاشة.**

- ❌ **مرفوض** (اختبار وهمي): `await expect(button).toBeVisible()` وخلاص.
- ✅ **مقبول** (اختبار حقيقي): تدوس على الزرار، وبعدين تتحقق من **الأثر الفعلي** — تغيّر في الداتا، ظهور/اختفاء عنصر تاني، رسالة خطأ صحيحة، تغيّر في Supabase، إلخ.

قبل ما تعتبر أي ميزة من القايمة تحت "✅ مُختبرة"، لازم الاختبار الخاص بيها يحقق **كل** المعايير دي:
1. بيعمل فعل حقيقي (click / fill / submit) مش بس يتحقق من وجود عنصر.
2. بيتحقق من نتيجة منطقية مرتبطة بالفعل ده تحديدًا (نجاح متوقع أو فشل متوقع، حسب السيناريو).
3. لو الميزة بترفض حالة غلط (مثلاً إيميل مش مسجّل، باسورد غلط، فورم ناقص)، لازم يكون فيه اختبار **يتأكد من الرفض فعليًا** (رسالة خطأ ظهرت، أو الفورم منعش يقفل)، مش بس اختبار للحالة الناجحة.
4. لو الميزة بتغيّر بيانات (إضافة/تعديل/حذف)، الاختبار يتأكد إن التغيير انعكس فعليًا (في الـ UI على الأقل، مش لازم يفتح Supabase مباشرة).

**لو Gemini مش قادر يكتب اختبار مستوفي الشروط دي لميزة معينة (مثلاً لأنها محتاجة داتا مش موجودة في حسابات الديمو، أو منطقها معقد)، الأصح إنه يسيبها ⬜ بدون اختبار، ومايكتبش اختبار سطحي وهمي بس عشان "يغطيها".**

---

## القايمة (يُحدَّث تلقائيًا بواسطة Gemini + مراجعة Claude يدويًا بين فترة وفترة)

### ✅ مُختبرة فعليًا (تستوفي القاعدة الذهبية)
- [x] تسجيل دخول أدمن ناجح (بيانات صح → يدخل الشاشة الرئيسية)
- [x] تسجيل دخول مدرّس ناجح (بيانات صح → تاب المدرّس ظاهر)
- [x] تسجيل دخول ولي أمر ناجح (بيانات صح → تاب ولي الأمر ظاهر)
- [x] الوضع الليلي: نقرة تضيف `data-theme=dark`، نقرة تانية تشيله (تحقق من حالتين، مش نقرة واحدة)
- [x] زرار إظهار الباسورد: نقرة تغيّر `type` من `password` لـ `text` فعليًا

### ⬜ لسه محتاجة اختبار حقيقي (أولوية عالية — منطق أعمال حساس)
- [ ] **تسجيل دخول ببيانات غلط** (إيميل مش موجود / باسورد غلط) → رسالة خطأ واضحة تظهر، ومايدخلش
- [ ] **إنشاء حساب بإيميل غير مسجّل مسبقًا** → لازم يترفض (ده قرار أمان أساسي في المشروع، رقم 4 في القرارات المتفق عليها)
- [ ] **إنشاء حساب بإيميل مسجّل فعليًا** → ينجح ويوصل لشاشة التفعيل
- [ ] **تطابق كلمة المرور في شاشة التسجيل** → لو الخانتين مش متطابقين، رسالة خطأ + منع الإرسال
- [ ] **إضافة طالب جديد بدون اختيار "مرحلة دراسية"** → لازم يترفض (ثغرة حقيقية اتصلحت، القسم 5.23 في PROJECT_HISTORY)
- [ ] **فلترة المجموعات حسب مرحلة الطالب** → بعد اختيار مرحلة معينة، قايمة المجموعات المعروضة تفضل بس اللي من نفس المرحلة
- [ ] **تحذير عدم تطابق تاريخ الميلاد مع المرحلة** → تاريخ ميلاد غير منطقي لمرحلة معينة يطلع تحذير (مش منع، تحذير بس)
- [ ] **ربط ولي أمر بطالب (بحث + اختيار)** → بعد الربط، الطالب يظهر فعليًا تحت حساب ولي الأمر ده
- [ ] **زرار الرجوع (Android/popstate) من شاشة محمية لشاشة عامة** → لازم يظهر مودال تأكيد، ولو أكّد يعمل logout حقيقي (مش بس يرجع بالعرض) — ده كان ثغرة أمنية حقيقية (القسم 5.6/5.7)
- [ ] **زرار الرجوع بين تابات نفس الدور** (مثلاً جوه شاشة المدرّس) → يرجع خطوة من غير ما يعمل logout غلط

### ⬜ لسه محتاجة اختبار حقيقي (أولوية متوسطة)
- [ ] **البحث العربي الذكي** (`normalizeArabic`/`matchesSearch`) → بحث بكلمة بأشكال مختلفة (همزة/تاء مربوطة) يرجع نفس النتيجة
- [ ] **توليد الإيميل التلقائي** → اسم بمسافات يتحول لإيميل بـ underscore صحيح
- [ ] **تغيير مالك الحساب** → بعد التغيير، الحساب القديم يفقد الصلاحية والجديد يكتسبها
- [ ] **مؤشر "مفعّل / لسه مش مفعّل"** → يعكس الحالة الفعلية من `profiles`/`claimed_by`
- [ ] **شارة "🔁 معيد"** → تظهر فقط للطلاب اللي عندهم `repeat_years > 0`

### ⬜ لسه محتاجة اختبار حقيقي (أولوية منخفضة / مؤجلة حتى تُبنى الميزة بالكامل)
- [ ] QR attendance (تسجيل حضور فعلي عبر مسح/كود، والتحقق إن السجل اتضاف)
- [ ] لوحة Super Admin: إضافة مؤسسة جديدة، أرشفة، فريق بصلاحيات
- [ ] عرض تفصيلي للفواتير (`center_invoices`) — لسه الشاشة نفسها مش موجودة

---

## ملاحظات لـ Gemini عند التحديث التلقائي

- لو لقيت ميزة جديدة في الكود مش موجودة في القايمة دي خالص، **ضيفها كسطر ⬜ جديد** في القسم المناسب (بدل ما تتجاهلها أو تكتب اختبار سطحي ليها فورًا).
- بلاش تحوّل أي سطر من ⬜ لـ [x] إلا لو كتبت الاختبار فعليًا وهو مستوفي القاعدة الذهبية فوق بالكامل.
- ركّز في كل مرة على أعلى سطر ⬜ متاح حسب الأولوية (عالية → متوسطة → منخفضة)، وماتحاولش تغطي كل حاجة في تشغيلة واحدة.
